node_modules/

# Rebuilt by `bun run snapshot`; large and stale within hours.
data/*.db
data/*.db-shm
data/*.db-wal

# Generated meal plans.
data/plan.json

# Recipe history is the one thing here that cannot be regenerated, so it is
# tracked rather than ignored.
!data/history.json
