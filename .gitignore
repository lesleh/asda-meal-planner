node_modules/

# Rebuilt by `bun run snapshot`; large and stale within hours.
data/*.db
data/*.db-shm
data/*.db-wal

# Generated meal plans and the approved snack list; regenerated each run.
data/plan.json
data/plan.md
data/snacks.json

# Leftovers carried between shops; rewritten by every plan.
data/carryover.json

# The snack blocklist and the dislikes list are personal taste; kept local so
# they never go public and never cause pull conflicts.
data/blocklist.json
data/dislikes.json

# Recipe history cannot be regenerated (it is hand-curated), so it is tracked.
!data/history.json
